# تجربه یادگیری RAG و چالش‌های پیاده‌سازی آن

در این پروژه با چند مفهوم جدید آشنا شدم که قبل از شروع کار تجربه‌ی زیادی باهاشون نداشتم.

### مفاهیم جدید

* **Admonition**
* **Descendants**
* **Decompose**
* **RAG**
* **Embedding**
* **Vector**
* **Vector Search**
* **Cosine Similarity**
* **Semantic Search**
* **Retrieval**
* **Top-k**
* **Grounding**
* **Hallucination**
* **pgvector**
* **MCP**
* **Idempotency**
* **Hybrid Search**
* **Reranking**
* **Query Expansion**
* خطای `psycopg2` هنگام ارسال Vector به PostgreSQL

برای اینکه این مفاهیم رو بهتر یاد بگیرم، ابتدا از AI خواستم هر کدوم رو **به زبان ساده و همراه با مثال و کاربرد واقعی** توضیح بده. بعد از اینکه با مفهوم کلی آشنا شدم، سعی کردم اون‌ها رو داخل پروژه ببینم و در عمل استفاده کنم.

 برای درک بهتر موضوع، از این منابع هم کمک گرفتم:
منابع:
برای mcp:
https://www.youtube.com/watch?v=eur8dUO9mvE
https://liara.ir/blog/%D8%AF%D8%B1%DA%A9-%D9%BE%D8%B1%D9%88%D8%AA%DA%A9%D9%84-%D8%B2%D9%85%DB%8C%D9%86%D9%87-%D9%85%D8%AF%D9%84-mcp/

برای RAG:
https://liara.ir/blog/rag-%DA%86%DB%8C%D8%B3%D8%AA%D8%9F-%D9%85%D8%B9%D8%B1%D9%81%DB%8C-retrieval-augmented-generation-%D9%88-%DA%A9%D8%A7%D8%B1%D8%A8%D8%B1%D8%AF%D9%87%D8%A7%DB%8C-%D8%A2%D9%86-%D8%AF%D8%B1-%D9%87%D9%88/
https://www.youtube.com/watch?v=1imP4X8fUdA

برای Vector Database:
https://www.youtube.com/watch?v=gl1r1XV0SLw
https://www.youtube.com/watch?v=klTvEwg3oJ4

برای Semantic Sreach:
https://www.youtube.com/shorts/cscPxq3zzg0
https://www.youtube.com/watch?v=BKbScJ2P2P0
---

## کجا گیر کردم و چطور حلش کردم؟

### مشکل اتصال PostgreSQL داخل Docker

در ابتدا کانتینر Docker بدون مشکل بالا می‌اومد، اما اتصال به PostgreSQL انجام نمی‌شد.

با بررسی بیشتر متوجه شدم که **PostgreSQL داخل Docker** و **PostgreSQL نصب‌شده روی سیستم** هر دو می‌خواستن از یک پورت استفاده کنن.

🔧 برای حل مشکل، پورت PostgreSQL در بخش `ports` فایل Docker را تغییر دادم تا تداخل برطرف بشه.

---

### مشکل مستندات خالی

مشکل بعدی این بود که بعضی از بخش‌هایی که از صفحات استخراج می‌شدن، عملاً متن قابل استفاده‌ای نداشتن.

برای حلش، شرطی به کد اضافه کردم که اگر تعداد کاراکترهای یک بخش از حد مشخصی کمتر بود، اون بخش نادیده گرفته بشه و به عنوان بخشی از Document ذخیره نشه.

این کار باعث شد داده‌ی ورودی تمیزتر بشه و از ذخیره‌ی بخش‌های بی‌ارزش جلوگیری بشه.

---

### بررسی توزیع داده قبل از Chunking

یکی از نکات مهمی که از بخش **Chunking** یاد گرفتم این بود که قبل از اینکه مستقیماً سراغ فیلتر کردن داده‌ها برم، بهتره ابتدا **توزیع داده‌ها** رو بررسی کنم.

یعنی به جای اینکه یک حد مشخص رو به صورت حدسی برای فیلتر کردن انتخاب کنم، اول ببینم داده‌ها چه توزیعی دارن و بعد براساس اون تصمیم بگیرم.

این موضوع کمک کرد تصمیم‌گیری درباره‌ی اندازه و کیفیت Chunkها منطقی‌تر باشه.

---

## خطای `vector <=> numeric[]` در PostgreSQL

یکی از خطاهایی که در زمان کار با Vectorها باهاش مواجه شدم:

```text
operator does not exist: vector <=> numeric[]
```

دلیل خطا این بود که `psycopg2` لیست Python رو به آرایه‌ی معمولی PostgreSQL یعنی `numeric[]` تبدیل می‌کنه، در حالی که عملگر `<=>` در `pgvector` انتظار داره هر دو مقدار از نوع `vector` باشن.

### 🔧 دو راه‌حل

برای حل این مشکل دو روش وجود داشت:

**روش اول — Cast مستقیم در SQL**

```sql
::vector
```

این روش ساده‌تره و نیاز به وابستگی اضافه‌ای نداره.

**روش دوم — استفاده از `pgvector.python`**

با استفاده از `register_vector` می‌شه کاری کرد که اتصال PostgreSQL نوع Vector رو به شکل مستقیم بشناسه.

### انتخاب من

در این پروژه روش اول رو انتخاب کردم:

> استفاده از `::vector` در SQL

چون برای این پروژه **ساده‌تر، شفاف‌تر و بدون وابستگی اضافی** بود.

---
### گیر: API سطح‌پایین MCP Server دکوریتور `list_tools` نداشت

کتابخانه‌ی `mcp` دو لایه دارد:

* `Server`: سطح‌پایین و مبتنی بر پروتکل
* `FastMCP`: سطح‌بالا و مبتنی بر ابزار

در `FastMCP`، تعریف هر ابزار خیلی ساده است: یک تابع پایتون با **type hint** و **docstring**. حتی `inputSchema` ابزار هم به‌صورت خودکار از روی همان type hintها ساخته می‌شود.

**درس مهم:** در این الگو، `docstring` و `type hint` فقط برای خوانایی یا مستندسازی نیستند؛ مستقیماً بخشی از قرارداد ابزار برای **مدل زبانی** هستند و به مدل کمک می‌کنند بفهمد ابزار چه ورودی‌هایی می‌گیرد و چه کاری انجام می‌دهد.



## چه چیزی هنوز برام مبهم هست؟

* سازوکار داخلی **HNSW** هنوز برام کاملاً روشن نیست. می‌دونم چطور از اون برای جست‌وجوی بردارها استفاده کنم، اما هنوز دقیقاً بررسی نکردم که گراف نزدیک‌ترین همسایه‌ها چگونه ساخته میشه و چرا جست‌وجوی اون تقریبیه.
* اینکه مدل **Embedding** دقیقاً چگونه مفهوم و معنای متن رو به یک بردار تبدیل میکنه هم هنوز برام مبهمه. میدونم متنهای مشابه معمولاً بردارهای نزدیک‌تری تولید میکنن، اما دلیل و سازوکار یادگیری این ارتباط معنایی رو عمیقاً بررسی نکردم.
* اگر این سیستم با **میلیون‌ها سند** در محیطی با Production اجرا بشه، هنوز نمی‌دونم **Re-indexing** رو چگونه باید بدون Downtime و بدون ایجاد ناسازگاری در نتایج جست‌وجو انجام داد.
* در صورت اتصال سیستم به یک **LLM**، هنوز نیاز دارم روش‌های کنترل **Hallucination** و طراحی Promptهایی رو بهتر یاد بگیرم تا مدل تا حد ممکن فقط بر اساس بخش‌های بازیابی‌شده پاسخ بدهد.

## اگر یک هفته‌ی دیگر وقت داشتم، سراغ چی می‌رفتم؟

اگر یک هفته‌ی دیگر برای توسعه‌ی این پروژه وقت داشتم، اول روی **یادگیری عمیق‌تر مفاهیم AI و RAG** تمرکز می‌کردم و بعد یک دوره‌ی فشرده آموزشی از مباحث مرتبط با Embedding، Vector Search، HNSW، RAG و LLMها تهیه میکردم.

بعد از اون، قابلیتهای بیشتری به پروژه اضافه می‌کردم. احتمالاً کل مستندات Django رو Scrape می‌کردم تا Dataset بزرگتر و کاملتری داشته باشم و مجموعه تستهای دقیقتر و پیچیده‌تری برای ارزیابی سیستم می‌نوشتم؛ مخصوصاً تست‌هایی که **Edge Case**های بیشتری رو در رفتار بات بررسی کنند.

در مرحله‌ی بعد، سراغ بهبود کیفیت مدل و بررسی روش‌هایی مثل **Fine-tuning** یا سایر روش‌های بهینه‌سازی مدل می‌رفتم تا ببینم چطور میشه کیفیت پاسخها و دقت بازیابی رو بیشتر کرد.
