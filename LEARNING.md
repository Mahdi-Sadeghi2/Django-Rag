# تجربه یادگیری RAG و چالش‌های پیاده‌سازی آن

در این پروژه با چند مفهوم جدید آشنا شدم که قبل از شروع کار تجربه‌ی زیادی باهاشون نداشتم.

### مفاهیم جدید

* **Admonition**
* **Descendants**
* **Decompose**
* **RAG**
* **Embedding**
* **Vector**
* **Vector Search**
* **Cosine Similarity**
* **Semantic Search**
* **Retrieval**
* **Top-k**
* **Grounding**
* **Hallucination**
* **pgvector**
* **MCP**
* **Idempotency**
* **Hybrid Search**
* **Reranking**
* **Query Expansion**
* خطای `psycopg2` هنگام ارسال Vector به PostgreSQL

برای اینکه این مفاهیم رو بهتر یاد بگیرم، ابتدا از AI خواستم هر کدوم رو **به زبان ساده و همراه با مثال و کاربرد واقعی** توضیح بده. بعد از اینکه با مفهوم کلی آشنا شدم، سعی کردم اون‌ها رو داخل پروژه ببینم و در عمل استفاده کنم.

 برای درک بهتر موضوع، از این ویدئوی یوتیوب هم کمک گرفتم:

https://www.youtube.com/watch?v=1imP4X8fUdA

---

## کجا گیر کردم و چطور حلش کردم؟

### مشکل اتصال PostgreSQL داخل Docker

در ابتدا کانتینر Docker بدون مشکل بالا می‌اومد، اما اتصال به PostgreSQL انجام نمی‌شد.

با بررسی بیشتر متوجه شدم که **PostgreSQL داخل Docker** و **PostgreSQL نصب‌شده روی سیستم** هر دو می‌خواستن از یک پورت استفاده کنن.

🔧 برای حل مشکل، پورت PostgreSQL در بخش `ports` فایل Docker را تغییر دادم تا تداخل برطرف بشه.

---

### مشکل مستندات خالی

مشکل بعدی این بود که بعضی از بخش‌هایی که از صفحات استخراج می‌شدن، عملاً متن قابل استفاده‌ای نداشتن.

برای حلش، شرطی به کد اضافه کردم که اگر تعداد کاراکترهای یک بخش از حد مشخصی کمتر بود، اون بخش نادیده گرفته بشه و به عنوان بخشی از Document ذخیره نشه.

این کار باعث شد داده‌ی ورودی تمیزتر بشه و از ذخیره‌ی بخش‌های بی‌ارزش جلوگیری بشه.

---

### بررسی توزیع داده قبل از Chunking

یکی از نکات مهمی که از بخش **Chunking** یاد گرفتم این بود که قبل از اینکه مستقیماً سراغ فیلتر کردن داده‌ها برم، بهتره ابتدا **توزیع داده‌ها** رو بررسی کنم.

یعنی به جای اینکه یک حد مشخص رو به صورت حدسی برای فیلتر کردن انتخاب کنم، اول ببینم داده‌ها چه توزیعی دارن و بعد براساس اون تصمیم بگیرم.

این موضوع کمک کرد تصمیم‌گیری درباره‌ی اندازه و کیفیت Chunkها منطقی‌تر باشه.

---

## خطای `vector <=> numeric[]` در PostgreSQL

یکی از خطاهایی که در زمان کار با Vectorها باهاش مواجه شدم:

```text
operator does not exist: vector <=> numeric[]
```

دلیل خطا این بود که `psycopg2` لیست Python رو به آرایه‌ی معمولی PostgreSQL یعنی `numeric[]` تبدیل می‌کنه، در حالی که عملگر `<=>` در `pgvector` انتظار داره هر دو مقدار از نوع `vector` باشن.

### 🔧 دو راه‌حل

برای حل این مشکل دو روش وجود داشت:

**روش اول — Cast مستقیم در SQL**

```sql
::vector
```

این روش ساده‌تره و نیاز به وابستگی اضافه‌ای نداره.

**روش دوم — استفاده از `pgvector.python`**

با استفاده از `register_vector` می‌شه کاری کرد که اتصال PostgreSQL نوع Vector رو به شکل مستقیم بشناسه.

### انتخاب من

در این پروژه روش اول رو انتخاب کردم:

> استفاده از `::vector` در SQL

چون برای این پروژه **ساده‌تر، شفاف‌تر و بدون وابستگی اضافی** بود.

---
